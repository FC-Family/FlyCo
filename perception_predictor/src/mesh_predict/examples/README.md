This directory keeps optional debug and visualization assets that are not part
of the official mesh prediction runtime surface.

- `launch/` contains non-official helper launches such as RViz visualization.
